pipeline {
  agent any

  stages {
    stage('terraform init') {
      steps {
        sh 'cd sandeepv_tf_jenkins'
        sh 'pwd'
        sh '/usr/local/bin/terraform init'
        sh '/usr/local/bin/terraform plan'
        sh '/usr/local/bin/terraform apply --auto-approve'
      }
    }
  }
}
